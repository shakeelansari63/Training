package ed.java.P016Enums;

public enum SimpleFlightCrew {
    // The values of enum are actual instances of Enum class which are accessed via Class like Static Members
    // E.g. SimpleFlightCrew.FLIGHT_ATTENDANT
    FLIGHT_ATTENDANT,
    SECURITY_OFFICER,
    COPILOT,
    PILOT,
}
